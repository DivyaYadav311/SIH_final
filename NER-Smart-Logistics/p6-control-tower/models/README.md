Local CLIP / Hugging Face weights are downloaded at runtime into the Hugging Face cache.

Do not commit `.bin`, `.safetensors`, or `.onnx` files here.

Set `HF_TOKEN` to use the Inference API without a local GPU.
